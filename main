우연히 함수 f(x)=(x-1)^((x-2)^(x-3))에서 x의 값이 1.24일 때 desmos.com에서는 실수, wolfram alpha에서는 복소수로 결과가 나옴을 발견했습니다.

또, 1.21, 1.22, 1.23은 desmos.com에서도 정의되지 않음으로 표기되었지만, 1.20, 1.24, 1.4와 같이 4의 배수 형태를 띄는 수는 실수로 표기되었습니다.
